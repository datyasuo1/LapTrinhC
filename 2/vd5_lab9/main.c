#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 
{
	int count = 1;
	while (count <= 10)
	{printf("\n this is interation %d\n", count);
	count++;
	}
	printf("\the ;oop is completed. \n");
	return 0;
}
